// use clap::Parser;
// use std::env;
// use std::fs;
// use std::fs::File;
// use std::io;
// use std::io::prelude::*;
// use std::io::BufReader;
// use std::process;

fn main() {
    lox_interpreter_rust::init();
}
